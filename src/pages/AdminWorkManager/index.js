import classNames from 'classnames/bind';
import styles from './AdminWorkManager.scss';

const cx = classNames.bind(styles);

function AdminWorkManager() {
    return <div> AdminWorkManager</div>;
}

export default AdminWorkManager;
